let person = {
    name: 'Mohamed',
    age: 30,
    location: 'Miami'  
};

console.log(`${person.name} is ${person.age} and is located in ${person.location} `);
person.age = person.age + 1;
console.log(`${person.name} is ${person.age} and is located in ${person.location} `);